/*function Tenta({curso, coordenador, data}){ 

    return(
        <>
        {curso.map((curso, i)=>(
            <tr key={i}><td>{i}</td><td>{curso}</td><td>{coordenador[i]}</td><td>{data[i]}</td><td><button >Remover</button></td></tr>
        ))}
        </>
    )
}
export default Tenta
///<td><button onClick={edit}>Editar</button></td>
*/