function Tenta({itens}){
    return(
        <>
        {itens.map((item)=>(
            <option>{item}</option>
        )
        )}
        </>
    )
}
export default Tenta
/*function Tenta({itens}){
    return(
        <>
        {itens.map((item)=>(
            <option>{item}</option>
        )
        )}
        </>
    )
}
export default Tenta*/